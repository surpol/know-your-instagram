<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Comments</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100 flex flex-col items-center p-5">

    <div class="comments-container w-full max-w-6xl">
        <h1 class="text-2xl text-gray-800 text-center mb-8">COMMENTS: <%= totalComments %></h1>
        <a href="/" class="text-blue-500 text-center block mb-6 hover:underline">← Back to main</a>

        <!-- Comments grid to display each comment in a grid layout -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% commentsList.forEach(function(comment) { %>
                <div class="comment-card bg-white p-5 rounded-lg shadow-md hover:shadow-lg transform hover:scale-105 transition duration-300 ease-in-out">
                    <div class="comment-content">
                        <!-- Create a clickable link to the media owner's Instagram page -->
                        <h4 class="font-bold text-lg mb-2">
                            <a href="https://www.instagram.com/<%= comment.mediaOwner %>" target="_blank" class="text-blue-500 hover:underline">
                                to: <%= comment.mediaOwner %>
                            </a>
                        </h4>
                        <p class="text-gray-600 mb-4"><%= comment.comment %></p>
                        <div class="text-right">
                            <span class="text-xs text-gray-500"><%= comment.time %></span>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

</body>
</html>
