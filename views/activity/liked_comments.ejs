<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liked Comments</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">

    <h1 class="text-center text-3xl lg:text-4xl font-bold text-purple-600 uppercase tracking-wide mb-8">Liked Comments: <%= likedCount %></h1>

    <div class="flex justify-center mb-6">
        <a href="/" class="text-purple-600 hover:text-purple-800 transition-colors text-lg">← Back to main</a>
    </div>

    <div class="overflow-x-auto">
        <table class="min-w-full max-w-3xl mx-auto bg-white shadow-lg rounded-lg overflow-hidden">
            <tbody>
                <% Object.keys(organizedLikes).forEach(function(title) { %>
                    <% organizedLikes[title].forEach(function(href) { %>
                        <tr class="hover:bg-gray-200 transition-colors">
                            <td class="p-4 border-b">
                                <h2 class="text-xl font-semibold">
                                    <a href="<%= href %>" target="_blank" class="text-purple-600 hover:underline">
                                        <%= title %>
                                    </a>
                                </h2>
                            </td>
                        </tr>
                    <% }); %>
                <% }); %>
            </tbody>
        </table>
    </div>

</body>
</html>
